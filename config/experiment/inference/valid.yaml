# @package _global_
predictor:
  test: false
  count_features_path: "/data/count_based_v1_candidates_valid.jsonl"
  limit: 10000
output_path: "/outputs/predictions_ensemble_valid_final"
limit: ${predictor.limit}
